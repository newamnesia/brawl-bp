# 服务端端口（默认 3001）
PORT=3001

# 生产环境（部署时设为 production）
NODE_ENV=production

# 前后端分离部署时，构建前端前设置 Socket.io 服务器地址
# VITE_SOCKET_URL=https://your-domain.com

# 首次部署时用于创建管理员账号；填写“盐:64字节十六进制摘要”格式的 scrypt 哈希
# 已有管理员账号的数据库不会因留空而被修改
ADMIN_PASSWORD_HASH=
